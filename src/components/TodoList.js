import React, { Component } from 'react';
import TodoItem from './TodoInput';

export default class TodoList extends Component {
    render() {
        return (
            <div>
                hello from todo TodoList
            </div>
        )
    }
}
